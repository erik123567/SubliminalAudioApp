platform :ios, '14.0'
require_relative '../node_modules/react-native/scripts/react_native_pods'
require_relative '../node_modules/@react-native-community/cli-platform-ios/native_modules'

target 'SubliminalAudioApp' do
  config = use_native_modules!

  use_react_native!(
    :path => config[:reactNativePath]
  )

  target 'SubliminalAudioAppTests' do
    inherit! :complete
  end

  # Override boost source and disable checksum verification
  pod 'boost', :podspec => 'https://raw.githubusercontent.com/CocoaPods/Specs/master/Specs/3/f/5/boost/1.76.0/boost.podspec'

  post_install do |installer|
    react_native_post_install(installer)
  end
end
